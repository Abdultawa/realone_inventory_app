<x-guest-layout>
    <div class="d-grid gap-2">

            <a href="{{route('register_with_nin')}}" class="btn btn-lg btn-success">Register with NIN</a>
            <a href="{{route('register_without_nin')}}" class="btn btn-lg btn-success">Register without NIN</a>
    </div>
</x-guest-layout>